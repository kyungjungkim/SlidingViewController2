//
//  Sliding.swift
//  Sliding
//
//  Created by Kyungjung Kim on 2019/12/21.
//  Copyright © 2019 prosoft. All rights reserved.
//

import UIKit

class Sliding {
    /*
    var menu: UITableView?
    var cell: MenuTableViewHeaderViewController?
    var cell1: MenuTableViewCell?
    
    
    var isMenuOpen: Bool = false

    @objc func menuOpenBtnClick(_ sender: Any) {
        if (isMenuOpen == false) {
            UIView.animate(withDuration: 0.8, animations: {
                self.menu!.frame = CGRect(x: 0.0, y: 30.0, width: UIScreen.main.bounds.size.width - 100.0, height: UIScreen.main.bounds.size.height - 30.0)
            }, completion: { finished in
                if (finished) {
                    self.cell!.uiBtnAll.frame = CGRect(x: self.cell!.uiBtnAll.frame.origin.x, y: self.cell!.uiBtnAll.frame.origin.y - 10.0, width: self.cell!.uiBtnAll.frame.width, height: self.cell!.uiBtnAll.frame.height)
                    
                    self.isMenuOpen = true
                }
            })
        }
    }
    
    @objc func swipeMenuClose() {
        if (isMenuOpen == true) {
            UIView.animate(withDuration: 0.8, animations: {
                self.menu!.frame = CGRect(x: -(UIScreen.main.bounds.size.width - 100.0), y: 30.0, width: UIScreen.main.bounds.size.width - 100.0, height: UIScreen.main.bounds.size.height - 30.0)
            }, completion: { finished in
                if (finished) {
                    self.cell!.uiBtnAll.frame = CGRect(x: self.cell!.uiBtnAll.frame.origin.x, y: self.cell!.uiBtnAll.frame.origin.y + 10.0, width: self.cell!.uiBtnAll.frame.width, height: self.cell!.uiBtnAll.frame.height)
                    
                    self.isMenuOpen = false
                }
            })
        }
    }
 */
}
